apiVersion: v1
kind: ServiceAccount
metadata:
  name: ticketing-api
  namespace: ticketing-system
  labels:
    app.kubernetes.io/part-of: ticketing-system
    app.kubernetes.io/component: api
  annotations:
    # 替换为 Terraform 的 api_secrets_irsa_role_arn 输出。
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/ticketing-system-dev-ticketing-api-role
automountServiceAccountToken: true

