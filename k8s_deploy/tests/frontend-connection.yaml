apiVersion: v1
kind: Pod
metadata:
  name: ticketing-system-frontend-test
  namespace: ticketing-system
  labels:
    app.kubernetes.io/part-of: ticketing-system
    app.kubernetes.io/component: frontend-test
spec:
  restartPolicy: Never
  containers:
    - name: frontend
      image: busybox:1.36
      command: ["wget"]
      args:
        - "-qO-"
        - http://ticketing-system-frontend:80/

