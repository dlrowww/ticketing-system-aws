apiVersion: v1
kind: Pod
metadata:
  name: ticketing-system-api-test
  namespace: ticketing-system
  labels:
    app.kubernetes.io/part-of: ticketing-system
    app.kubernetes.io/component: api-test
spec:
  restartPolicy: Never
  containers:
    - name: api
      image: busybox:1.36
      command: ["wget"]
      args:
        - "-qO-"
        - http://api:8080/health

