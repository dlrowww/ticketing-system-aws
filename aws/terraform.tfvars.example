# Copy this file to a local environment-specific tfvars file, then review every
# value before terraform plan/apply. No secret values belong in this file.

aws_region   = "us-east-1"
environment  = "dev"
project_name = "ticketing-system"
owner        = "simon"

# Small development defaults. Use Multi-AZ, deletion protection and a suitable
# instance size before treating this as production.
db_instance_class        = "db.t4g.micro"
db_multi_az              = false
db_deletion_protection   = false
db_skip_final_snapshot   = true

# With these values left unset, environment = "dev" automatically disables RDS
# automated backups, enables ECR force deletion, and uses a 0-day application
# Secret recovery window. Staging/production default to 7 backup/recovery days
# and disabled ECR force deletion. Override only for a deliberate policy.
# db_backup_retention_days                  = 0
# ecr_force_delete                           = true
# application_secrets_recovery_window_in_days = 0

enable_cloudwatch_observability = true
cloudwatch_log_retention_days   = 30

# Pin the operator chart deliberately; review its release notes before upgrades.
external_secrets_chart_version = "2.7.0"
external_dns_chart_version     = "1.21.1"

# Enable these two lines only after a real public Route 53 hosted zone exists.
# route53_zone_id        = "Z0123456789EXAMPLE"
# application_domain_name = "tickets.example.com"

# When both DNS values are set, ExternalDNS automatically observes the Ingress
# and maintains its Route 53 Alias after the ALB is created.
